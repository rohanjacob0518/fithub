import { title } from "@/components/primitives";

export default function PricingPage() {
	return (
		<div>
			<h1 className={title()}
			style={{
				color:"red"
			}}
			>Pricing</h1>
			<h1 className={title()}
			style={{
				color:"green"
			}}
			>Pricing</h1>
		</div>
	);
}
